# +required Version of the endpoint configuration YAML
version: 0.1

# +required List of endpoints to create
endpoints:
  # +required Unique name for the endpoint. (This name will be used when generating the managed API)
  - name: X1
    # +required Numeric port value that gets exposed via this endpoint
    port: 3001
    # +required Type of the traffic this endpoint is accepting. Example: REST, GraphQL, etc.
    # Allowed values: REST, GraphQL, GRPC
    type: REST
    # +optional Network level visibility of this endpoint. Defaults to Project
    # Accepted values: Project|Organization|Public.
    networkVisibility: Organization
    # +optional Context (base path) of the API that is exposed via this endpoint.
    # This is mandatory if the endpoint type is set to REST or GraphQL.
    context: /basepath1
    # +optional Path to the schema definition file. Defaults to wild card route if not provided
    # This is only applicable to REST endpoint types.
    # The path should be relative to the docker context.
    schemaFilePath: openapi.yaml
connections:
  # - id: choreo://<Project>HR/<Service>EmployeeService/<Version>v1/<Endpoint>endpoint
  - id: choreo://A/X1/v1/basepath1
    mappings:
      - service.url: X1_SVC_URL
      - consumer.secret: X1_SVC_CONSUMER_SECRET
      - consumer.key: X1_SVC_CONSUMER_KEY
      - token.url: X1_SVC_TOKEN_URL
Config:
 EnvVariables:
   - LOG_LEVEL = ERROR
   - THREAD_POOL_SIZE = 10
